{
  "name": "auto_text_crm_dockerized",
  "version": "1.0.0",
  "description": "## Setup Instructions 1. `pip install -r requirements.txt` 2. Configure `.env` file 3. Run migrations: `python manage.py migrate` 4. Start Redis 5. Start Celery worker & beat 6. Run the Django server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "react-scripts start",
  "build": "react-scripts build",
  "test": "react-scripts test",
  "eject": "react-scripts eject"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.8.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "react-calendar": "^5.1.0",
    "react-scripts": "^5.0.1",
    "sqlite3": "^5.1.7",
    "twilio": "^4.11.0"
  }
}
